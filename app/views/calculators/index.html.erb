<p id="notice"><%= notice %></p>
<% require 'net/http'
require 'json'

url = 'http://localhost:3000/markets'
uri = URI(url)
markets = Net::HTTP.get(uri)
hmarkets = JSON.parse(markets)

url = 'http://localhost:3000/rates'
uri = URI(url)
rates = Net::HTTP.get(uri)
hrates = JSON.parse(rates)

url = 'http://localhost:3000/currencies'
uri = URI(url)
currencies = Net::HTTP.get(uri)
hcurrencies = JSON.parse(currencies)

rcurrencies = hcurrencies.reverse

%>

<h1>Calculator</h1>
<p>Currency </p>
<form onchange="myFunction()">
  <select id="market">
    <%hmarkets.each do |mar| %>
    <option>
        <% if mar["base_currency_id"] < mar["quoted_currency_id"] %>
          <% hcurrencies.each do |curr| %>
            <% if curr["id"] == mar["base_currency_id"] %>
            <%= curr["currency_name"] %> /
            <% end %>
            <% if curr["id"] == mar["quoted_currency_id"] %>
            <%= curr["currency_name"] %>
            <% end %>
          <% end %>
          <% end %>

<% if mar["base_currency_id"] > mar["quoted_currency_id"] %>
    <% rcurrencies.each do |rcurr| %>
        <% if rcurr["id"] == mar["base_currency_id"] %>
          <%= rcurr["currency_name"] %> /
          <% end %>
          <% if rcurr["id"] == mar["quoted_currency_id"] %>
            <%= rcurr["currency_name"] %>
          <% end %>
          <% end %>
          <% end %>
    </option>
    <% end %>
      </select>
      <p id="bc"></p>
      <br><input type="text" name="bcurrency" id="bcurr" >
      <br>
      <br>
      <p id="qc"></p>
      <input type="text" name="qcurrency" id="qcurr" >
    </form>

<button onclick="myFunction()">Try it</button>

<p id="demo"></p>
<p id="demo2">ruby</p>
<p id="demo1"></p>
<p id="demo3"></p>



<script>
function myFunction() {
  var rate;

  var x = (document.getElementById("market").value).split(" / ");
  var bc_id = x[0];
  var qc_id = x[1];
  document.getElementById("bc").innerHTML = bc_id;
  document.getElementById("qc").innerHTML = qc_id;

  var qamount = parseFloat(document.getElementById("qcurr").value);
  var bamount = parseFloat(document.getElementById("bcurr").value);
  

  var request = new XMLHttpRequest();

  request.open('GET', 'http://localhost:3000/currencies', true);
  request.onload = function () {

  // Begin accessing JSON data here
  var cdata = JSON.parse(this.response);

  var request2 = new XMLHttpRequest();

  request2.open('GET', 'http://localhost:3000/rates', true);
  request2.onload = function () {


  // Begin accessing JSON data here
  var rdata = JSON.parse(this.response);

  var request3 = new XMLHttpRequest();

request3.open('GET', 'http://localhost:3000/markets', true);
request3.onload = function () {

  // Begin accessing JSON data here
  var mdata = JSON.parse(this.response);


  cdata.forEach(getcid);
  var bid;
  var qid;
  var mid;
  


  function getcid(cobj){
    if (cobj["currency_name"] == x[0]){
      bid = cobj["id"];
    }
    if (cobj["currency_name"] == x[1]){
      qid = cobj["id"];
    }

  }
      mdata.forEach(getmid);
    function getmid(mobj){
      if ((mobj["base_currency_id"] == bid) && (mobj["quoted_currency_id"] == qid)){
        mid = mobj["id"];
      }


      rdata.forEach(getrate);
      function getrate(robj){
        if (robj["market_id"] == mid){
          rate = robj["price"];
        }
      }
      rate = rate/100 + 1;
      qamount = bamount * rate;
      document.getElementById("qcurr").value = qamount;

      bamount = qamount / rate;
      document.getElementById("bcurr").value = bamount;

  }  

  };

  request3.send();
};

request2.send();
  };

request.send();
};
  </script>