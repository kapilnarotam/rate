<%= form_with(model: rat, local: true) do |form| %>
  <% if rat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rat.errors.count, "error") %> prohibited this rat from being saved:</h2>

      <ul>
      <% rat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
